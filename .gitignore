# Source assets (scanned books / images) — not version controlled
/assets/

# --- Python / Django backend ---
backend/.venv/
.venv/
__pycache__/
*.py[cod]
*$py.class

# Secrets & local config
.env
backend/.env

# Django runtime artifacts
backend/staticfiles/
*.log
*.sqlite3

# Vector store (persistent ChromaDB)
/chroma/

# Uploaded materials + their OCR output (user content, not version controlled)
/uploads/

# OCR output (clean structured PDFs produced from scanned sources)
/results/

# Tesseract language packs downloaded by gpu_ocr_books.py (cache)
/tessdata/

